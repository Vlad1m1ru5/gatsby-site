import React from 'react'

const NotFoundPage: React.FunctionComponent = () => (
  <section>
    <h2>Error has happened!</h2>
    <pre>Sorry, this page is not found.</pre>
  </section>
)

export default NotFoundPage